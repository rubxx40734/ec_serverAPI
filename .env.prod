MODE=prod
# ----- MySQL Container Settings -----
MYSQL_DATABASE=store
MYSQL_USER=bill
MYSQL_PASSWORD=Bill.60800
MYSQL_ROOT_PASSWORD=1111

# ----- NestJS Application Settings -----
DB_HOST=35.224.250.145
DB_PORT=3306
# NestJS 會直接從上面的變數讀取使用者和密碼，所以這裡不需要重複
# DB_USER=${MYSQL_USER} # 這種語法在 .env 中不一定通用，我們讓程式去讀
# DB_PASSWORD=${MYSQL_PASSWORD}
# DB_DATABASE=${MYSQL_DATABASE}

# --- JWT (for Auth module later) ---
JWT_SECRET=HelloWord
JWT_EXPIRES_IN=7d